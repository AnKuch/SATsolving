#include <iostream>
#include <fstream>
#include <string>
#include <limits>

using namespace std;

string oneOnEach (char a[][][],int zeilen,int spalten)
{
    string clause
    for (int i=0; i<=zeilen; i++)
    {
        for (int j=0; i<=spalten; j++)
        {
            for (int c=1; c<=3; c++)
            {
                switch (a[i][j][c])
                    {
                        case
                    }
            }
        }
    }
}
